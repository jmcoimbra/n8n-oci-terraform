# Copie para .env (fica no .gitignore) apenas se for rodar localmente pra estudo.
# Em produção (na VM) os secrets vêm do OCI Vault via cloud-init.

DOMAIN=n8n.meusite.com.br
ACME_EMAIL=voce@exemplo.com
N8N_BASIC_AUTH_USER=admin
